name: CMake CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    # Matrix strategy: run on multiple OSes and build types
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        build_type: [Debug, Release]

    runs-on: ${{ matrix.os }}

    steps:
    - name: Checkout source
      uses: actions/checkout@v4

    # Linux/macOS: install dependencies
    - name: Install dependencies (Linux/macOS)
      if: runner.os != 'Windows'
      run: |
        sudo apt-get update || true
        if [ "$RUNNER_OS" = "Linux" ]; then
          sudo apt-get install -y cmake g++ make valgrind cargo python3-pip emscripten nodejs
        fi
        if [ "$RUNNER_OS" = "macOS" ]; then
          brew install cmake rust python emscripten nodejs || true
        fi
        python3 -m venv test
        . test/bin/activate
        pip3 install pytest

    - name: Configure
      run: cmake -S . -B build -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}

    - name: Build
      run: cmake --build build --verbose --config ${{ matrix.build_type }}

    - name: Run comparison tests
      run: ctest -L cmp --test-dir build --output-on-failure --build-config ${{ matrix.build_type }}

    - name: Run memory tests
      if: runner.os != 'macOS'
      run: ctest -T memcheck -L memcheck --test-dir build --output-on-failure --build-config ${{ matrix.build_type }}

    - name: Run memory tests fallback
      if: failure()
      run: cat /home/runner/work/b12od/b12od/build/Testing/Temporary/{MemoryChecker.,LastDynamicAnalysis}*.log

    - name: Build Rust
      run: cd rust && cargo build --verbose

    - name: Run Rust tests
      run: rm -f build/libb12od.dylib && cd rust && DYLD_FALLBACK_LIBRARY_PATH=target/debug cargo test --lib -v

    - name: Build Python
      run: . test/bin/activate && cd python && pip install . --verbose

    - name: Run Python tests
      run: . test/bin/activate && cd python && pytest --verbose

    - name: Build WASM
      run: cd wasm && emcmake cmake -S . -B build && cmake --build build --verbose --config ${{ matrix.build_type }} && cmake --install build --prefix . --config ${{ matrix.build_type }}

    - name: Run WASM tests
      run: cd wasm && node test.js
